<!-- 文件用途：说明双语静态个人站的预览、构建、内容维护与验证方法。 -->
# 梁彦诗 · Doris Liang

一个研究优先的中英文个人站。保留首页开场，作品、文章、荣誉和关于页面有独立地址，成长时间线集中在首页。原生 JavaScript + Node 静态生成，正文由静态 HTML 提供；桌面鼠标增强按需加载本地托管的 Cuberto Mouse Follower 与 GSAP。

## 本地预览

需要 Node.js 22+、Python 3。运行：

```powershell
pnpm dev
```

命令先生成 `dist/`，随后仅在本机启动 <http://127.0.0.1:4173/>。英文版为 <http://127.0.0.1:4173/en/>。修改内容后运行 `pnpm build` 并刷新浏览器；开发服务器不自动构建。也可不用 pnpm，直接执行 `node scripts/build.mjs`，然后执行 `python -m http.server 4173 --bind 127.0.0.1 --directory dist`。

不要在仓库根目录启动静态服务器：页面 HTML、RSS、sitemap 和 robots.txt 均由构建生成到 `dist/`。

## 检查与发布

```powershell
pnpm lint
pnpm typecheck
pnpm test
```

- `lint`：维护中文件的说明、旧卡片样式残留、焦点与减少动效规则。
- `typecheck`：兼容旧命令名，实际执行 `node --check` 语法检查，不是静态类型检查。
- `test`：自动重新构建，检查 22 页正文 HTML、2 个旧轨迹重定向、全部站内路径与锚点、双语互链、八项荣誉、旧 URL 兼容，以及中文文章 SHA-256 完整性。
- `build`：单独生成 `dist/`，不启动服务器。
- `screenshots`：可选的 Playwright 截图脚本，需要开发环境另外提供 `playwright` 及 Chromium；不属于默认构建依赖。本次验收使用 Codex 浏览器，截图见 `artifacts/redesign/`。

GitHub Pages 工作流发布 `dist/`，对 `main` 的推送会触发部署。分支上的本地构建不会发布网站。

## 页面与内容维护

中文使用 `/`，英文使用 `/en/`；两种语言均有 `projects/`、五个项目详情、`notes/`、文章详情、`honors/`、`about/`。

- `content/site-data.js`：已提供的中文个人资料、项目、教育、实践、技能和文学原文。
- `content/locales.js`：英文事实表述、中英文界面、自我介绍与项目身份；新增内容时同步两个语言版本。
- `content/portfolio.js`、`src/portfolio.js`：个人介绍分段、研究与实践分组、项目摘要及证据的阅读层次。
- `content/honors.js`：荣誉正式名称、年月、类别、证书路径与文章关联。
- `src/pages.js`、`src/components.js`：静态页面与共享组件；`src/hero.js` 保留原有首页开场。
- `src/timeline.js`：首页年度时间线与机构标识，沿用已有经历数据和项目详情入口。
- `src/honors.js`：按年份组织独立荣誉页、作品与证书互链；《灵语》封面为用户提供。
- `src/routes.js`：路径约定、生产域名与旧链接兼容。
- `app.js`、`theme-init.js`：主题、手机导航和语言锚点；正文不依赖浏览器脚本。

《未选择的路》写作日期为 2025.01，获奖日期为 2025.04，分别展示。英文页翻译标题、简介与获奖信息，正文保留中文并标明原作语言。文学正文如经作者主动修订，需要同步更新完整性测试的期望摘要。

个人图片来自已有素材，首页机构标识来自各机构官网，来源记录在 THIRD_PARTY_NOTICES.md。荣誉证书可通过链接打开，志愿服务证书直接展示。全运会志愿服务放在关于页，不纳入八项荣誉。未提供的图片、日期与个人经历不得虚构。

旧的 `/?note=unselected-road#top` 和首页栏目锚点仍可访问，由浏览器脚本导向新页面。新页面可直接访问、刷新和分享；没有 JavaScript 时正文及普通链接仍可用，旧链接迁移和交互增强需要 JavaScript。

## 设计与验收

内容区最大宽度 1040px，阅读区 700px，使用暖白、柔和玫瑰色和细分隔线。网页界面与大开场统一复用 LivePilot-v2 网页的 LiveNest Sans SC 本地字体，文学原文保留衬线字体。首页先呈现背景、兴趣与生活的自然段介绍，再通过一条带日期、官方机构标识或主题图标的时间线呈现教育、研究、实习、教学与创作。作品按研究、产品与工程实践分组；日期、论文身份、个人工作与成果放在同一阅读区域，GRACE 的录用信息、第 5 作者及结果数字直接可见。标题和明确的论文／详情链接承担跳转，支持独立键盘访问。首页整合原轨迹页的年度导航与分年时间轴，按 2026、2025、2024 倒序展示；保留机构图标，补齐升途授课经历、研究项目时间与 ITSO 工作细节，手机采用纵向年份导航和紧凑图文布局。详细约束和验证记录见 [设计说明](docs/个人站改版.md) 与 [验收记录](docs/改版验收.md)。

既有 `design-qa.md`、`hero-source-audit.md` 及 font/hero 比较脚本是上一版历史记录，不代表新版结构。参考设计归属见 `THIRD_PARTY_NOTICES.md`；没有复制 Innei 的个人内容或图片。

## 社交资料与交互增强

- 顶部导航新增荣誉入口，独立荣誉页按年份倒序展示八项获奖记录，链接已有证书和文学原文；首页以封面入口连接荣誉页。
- 首页开场、关于页和页脚提供 LinkedIn 链接，首页自我介绍下方不重复社交入口。抖音创作介绍包含账号 `92689179314`、2026-09-18 突破 1 万粉丝的记录、原始抖音码和里程碑图片，抖音码和纪念图直接在关于页展示。志愿服务证书同样直接展示，桌面与说明并排、手机纵向排列。
- `content/experience.js` 统一维护 RA、ITSO、深圳市大数据研究院实习与 ECE2050 USTF，供首页使用；`content/social.js` 维护账号信息。详细资料依据见 [本轮资料记录](docs/资料与动效更新.md)。
- `src/motion.js` 接入本地固定版本 Cuberto Mouse Follower 1.2.1 与 GSAP 3.15.0：平滑跟随、轻微速度形变、链接柔和反馈、首页社交按钮磁吸。首次鼠标移动后加载；触屏与减少动态效果回退系统鼠标；桌面加载或失焦期间保留同款静态小圆点，滚轮、重新进入页面及恢复焦点均可唤醒动效，空闲暂停渲染。已移除旧星星拖尾和静态圆环。
- GRACE 项目可通过 `pdfUrl` 配置直达全文的链接；列表和详情共用，开场不增加论文按钮。目前尚未取得可公开的 PDF，保留准确的 DAC 介绍页链接，不把介绍页标为 PDF。

关于页仅保留技能、志愿服务证书、抖音图片展示和联系信息；自我介绍、研究工作教学与教育经历集中在首页。

独立轨迹页已取消，导航与页脚不再提供入口。旧 `/journey/`、`/en/journey/` 自动转到对应语言首页时间线，已有 `#year-2024` 至 `#year-2026` 锚点保留；无 JavaScript 时转到时间线起点。重定向页不进入 sitemap。

作品页收录 Threadline 个人任务与时间工作台，并生成中英文详情页，附项目演示截图、GitHub 仓库和 Windows Release 入口。介绍依据项目 README；开始日期未确认，仅标注持续开发中。

作品列表采用“标题 → 状态与时间 → 简介 → 链接”的顺序；移除标题上方重复的分类小字，状态和时间使用 16px 字号，手机自动换行。

页脚不展示 RSS 入口，订阅文件与自动发现元信息仍保留。关于页抖音介绍直接以账号名称开头，移除标题上方的装饰小字；中英文同步。

校园生活区移除重复的兴趣介绍；志愿服务证书改为 144px 宽的缩略图，靠近服务信息，点击可查看原图，避免大图撑高整行。手机按文字、证书纵向排列。
